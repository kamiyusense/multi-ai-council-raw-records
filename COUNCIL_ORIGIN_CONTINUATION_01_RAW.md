Google JulesおよびGeminiエコシステムを中心に、他社AIとの協調を含めた自律開発司令塔の設計について調査・検証しました。

### 1. Julesを外部司令塔から操作する最適な公式手段

公式の\*\*Jules REST API (`[https://jules.googleapis.com/v1alpha/](https://jules.googleapis.com/v1alpha/)`)\*\*を利用するのが最適です。 事前にJules Web UI側でGitHubリポジトリをSourceとして接続し、Settingsから発行したAPIキー（`X-Goog-Api-Key`）を用いてリクエストを送信します。

### 2. Jules APIの機能検証

- **task/session作成**: **可能**（`POST /v1alpha/sessions`。プロンプト、対象ブランチ、リポジトリSourceを指定）
- **指示送信**: **可能**（`POST /v1alpha/sessions/{id}:sendMessage`）
- **plan取得/承認**: **可能**（`sessions.get` でplan状態を確認し、`POST /v1alpha/sessions/{id}:approvePlan` で承認。セッション作成時に `requirePlanApproval: true` で明示制御可能）
- **progress/activity取得**: **可能**（`GET /v1alpha/sessions/{id}/activities`）
- **diff/code changes取得**: **部分的に可能**（Activityのメタデータとしてファイル変更履歴が流れますが、完全なGit diffはGitHub側のブランチや生成PRから取得する方が確実）
- **test/bash output取得**: **活動ログ経由のみ**（JulesがVM内で実行したテスト結果のサマリーはActivity内に含まれますが、任意のbashコマンドをリモートから直接叩く機能は非公開）
- **completion取得**: **可能**（SessionのステータスまたはPR作成Activityの完了で検知）
- **追加指示**: **可能**（`sendMessage` により会話形式で修正指示を継続可能）
- **中断/停止**: **制限あり**（公式APIに `abort` / `cancel` エンドポイントは現在明示されておらず、Sessionを放置するかGitHub側でPR/ブランチを閉じる運用が基本）

### 3. Jules開発中のGeminiによる監視

**可能**です。Jules APIの `activities.list` を司令塔がポーリングし、新しいアクティビティ（プラン策定、編集ファイル、実行結果サマリー）が生成されるたびにGemini APIへ送り、妥当性判定を行う設計が成立します。

### 4. Gemini API無料Tierの監視・レビュー利用

**可能**です。軽量なFlashモデル（Gemini 2.5 Flash / Flash-Lite等）を監視・一次フィルタとして利用できます。ただし、10項で述べるプライバシー制約への配慮が不可欠です。

### 5. 一般向けGemini Chat無料枠とGemini API無料Tier

**完全に別物**です。

- **Gemini Chat (gemini.google.com)**: 一般消費者向けWebサービス（Googleアカウント紐付け）。
- **Gemini API (Google AI Studio / Vertex AI)**: 開発者向けプログラマティックインターフェース（APIキー紐付け）。

### 6. 一般向けGemini Chat無料枠の自作プログラムからの正式利用

**存在しません**。Web画面のスクレイピングやセッショントークンのリバースエンジニアリングは利用規約（ToS）違反となります。プログラムからの利用はGoogle AI StudioまたはVertex AIのAPI利用が唯一の公式手段です。

### 7. Julesの利用枠とJules API利用量の関係

Jules API経由のセッション作成も、Julesサービス基盤上のタスク上限・同時実行数枠（日次制限）を直接消費します。

### 8. Jules APIとWeb UIの利用枠共有

**共有されます**。APIキーはJules Web UIのアカウント設定から発行されるため、同一のクォータプールを消費します。

### 9. Gemini API無料Tierの制限目安

無料Tier（Google AI Studio）は**15 RPM（Requests Per Minute）**、**100万 TPM（Tokens Per Minute）**、\*\*1,500 RPD（Requests Per Day）\*\*程度が標準的な上限です（モデル世代により微小な差異あり）。1日1,500回の上限があるため、細かい監視ログを全件無加工で投げる構成では枯渇します。

### 10. コード・非公開repo送信時のデータ利用・プライバシー

- **Google AI Studio 無料Tier**: 入力プロンプトおよびコードデータは**Googleのモデル改善・学習に利用される可能性があり、人間のレビュアーに閲覧される場合**があります。機密コードや非公開リポジトリの送信は重大な漏洩リスクになります。
- **有料Tier（従量課金）または Vertex AI**: 顧客データはモデル学習に使用されず、データ境界が保護されます。企業・私有コードを扱う場合は有料Tierへの切り替えが必須です。

### 協調監視ワークフロー（11〜14の統合設計）

&#x20;[開発タスク投入]        │        ▼  ┌───────────────┐  │ 司令塔オーケストレータ │ ─── (GitHub Webhook / Jules API)  └───────┬───────┘          │  ┌───────┴───────┬──────────────┐  ▼               ▼              ▼ Claude Code    OpenAI Codex   Google Jules (ローカルCLI/Agent) (CLI/Agent)   (Cloud Agent: git branch切断)          │               │              │          └───────┬───────┴──────────────┘                  │ (Git Commit / Push / Activityログ)                  ▼  ┌────────────────────────────────────────────────┐  │ 【第1段階】Deterministic Rules (静的解析/正規表現)  │  │ ・.env, 鍵情報, rm -rf, drop tableの検知        │  │ ・許可パス外の書き換え遮断                      │  └───────────────────────┬────────────────────────┘                          │ 通過                          ▼  ┌────────────────────────────────────────────────┐  │ 【第2段階】軽量AI一次監視 (Gemini 2.5 Flash Lite) │  │ ・差分の要約とタスク逸脱（仕様違反）の検知      │  │ ・レート制限節約のため1commit単位に束ねて判定   │  └───────────────────────┬────────────────────────┘                          │ 疑義/重要変更                          ▼  ┌────────────────────────────────────────────────┐  │ 【第3段階】高精度レビュー (Gemini Pro / Claude)   │  │ ・アーキテクチャ整合性、セキュリティ詳細判定     │  └───────────────────────┬────────────────────────┘                          │ Critical判定                          ▼  ┌────────────────────────────────────────────────┐  │ 【第4段階】人間承認 (Slack / GitHub PR Review)   │  └────────────────────────────────────────────────┘ &#x20;

- **Julesの監視と緊急停止（12, 13）**: Jules API作成時に `requirePlanApproval: true` を設定することで、計画段階で一度実行が止まります。活動中に危険な変更を検知した場合は、Jules側でなく**GitHub側で対象ブランチを削除/保護するか、WebhookでPRを即時クローズする**ことで、ダウンストリームへの破壊的影響を物理的に遮断します。
- **段階制レビュー（14）**: API上限とコストを守るため、静的チェック（AST解析、linter、シークレットスキャン）で80%の不要な検証を落とし、軽量モデルで変更の文脈を評価、高難度な設計妥当性判定のみPro/Opusクラスへルーティングします。

### 15. 見落とされがちなGoogle側の有効サービス

- **Cloud Run Functions / Eventarc**: GitHub WebhookやJules APIのポーリング結果を受け、監視スクリプトをサーバーレスで動かす低遅延ハブとして最適。
- **Gemini Context Caching**: リポジトリ全体のツリー構造やコーディング規約（5〜10万トークン）をあらかじめキャッシュしておくことで、レビュー時のレイテンシと入力コストを大幅に削減可能。
- **Secret Manager**: 各AIのAPIキー（GitHub Token, Jules Key, OpenAI Key, Claude Key）の安全な一元管理に必須。

### Google/Gemini視点での推奨アーキテクチャ設計

各ツールの特性をフラットに評価すると、\*\*「全作業を1つのAIで完結させない適材適所のハイブリッド構成」\*\*が最も堅牢です。

役割\
最適な選定\
理由

**ローカル密着実装**\
**Claude Code**\
ターミナル操作、局所的な即時リファクタリング、既存ファイル群の微修正に最も応答性が高い。

**非同期バックグラウンド開発**\
**Google Jules**\
独立したIssue対応や依存関係更新、テスト追加などをクラウド完結でPR化させ、ローカルリソースを占有しない。

**全体統合・監視・長期記憶**\
**Gemini (Flash / Pro)**\
巨大コンテキストウィンドウ（1M〜2Mトークン）とコンテキストキャッシュにより、リポジトリ全体像を把握した監視・差分レビュー役として最も費用対効果が高い。

**ハブ・司令塔**\
**GitHub Actions + 軽量Python/Nodeオーケストレータ**\
独自エージェント間通信プロトコルを作るより、GitHubのPR/Issue/Reviewインターフェースを単一の真実（Single Source of Truth）にするのが最も破綻しにくい。

**具体的な運用の流れ**:

1. 司令塔にタスクを投げると、「非同期なサブタスク（テスト補強・定型修正）」はJules APIへ、「複雑な設計変更」はローカルのClaude Code / Codexへ振り分けます。
2. 各AIは必ず作業ブランチを作成してプッシュします。
3. 司令塔（GitHub Webhook連携）が起動し、静的検証 → Gemini Flashによる一次チェックを実施します。
4. 問題がなければPRが作成され、破壊的変更や疑義フラグが付いた場合のみ、人間に通知が届きワンクリックでマージ/差戻しを行います。

これによって、人間は「コードのコピペ」から完全に解放され、上位の要件定義と最終承認だけに専念できます。

展開

お、Gemini案きたな。**ちゃぴお案を先に固定しておいて正解だった。**\
まだClaude案を見る前なので、ここでは最終結論には混ぜず、**Gemini案を「第2案」としてそのまま評価して固定**する。

かなり面白い。ワイとGemini、独立に考えた割に骨格はだいぶ一致してる。

| 論点ちゃぴお案Gemini案  |               |                              |
| --------------- | ------------- | ---------------------------- |
| Jules操作         | REST API      | REST API                     |
| 人力コピペ廃止         | ○             | ○                            |
| 監視方式            | 段階制           | 段階制                          |
| L0              | deterministic | deterministic                |
| L1              | Gemini Free候補 | Gemini Flash/Lite            |
| 高難度             | Claude/Codex等 | Gemini Pro/Claude            |
| 人間              | 高リスク時のみ       | Critical時のみ                  |
| GitHub          | 成果物の基準点       | **Single Source of Truth寄り** |
| 常時3AI監視         | 否定的           | 実質否定的                        |
| Gemini Chat無料枠  | 使わない          | 公式利用不可                       |
| Gemini API Free | 監視に使える        | 監視に使える                       |
| 秘密repo→Free     | 最小限推奨         | **かなり否定的**                   |

### Geminiから出た「ちゃぴお案になかった/弱かった」ポイント

一番いい追加情報は**Julesの停止能力の弱さ**。

Gemini調査では、Jules APIに明示的な`abort/cancel`が見当たらないとしてる。だから、

**「監視AIがBLOCKした → Julesそのものを即kill」**

を前提に設計するのは危険。

代わりに、最初から隔離branchで作業させて、

**ヤバければPRを閉じる / branchを捨てる / protected mainへ絶対入れない**

という**封じ込め型安全設計**にする。

これかなり重要。\
「AIを止める」より\*\*「AIが暴れても本体には届かない」\*\*を基本思想にした方が強い。

もう一個いいのが**commit単位で監査する**案。

ワイは「怪しいイベントだけGemini」と考えてたけど、

> deterministicで逐次監視\
> ↓\
> 変更をcommit/diff単位にまとめる\
> ↓\
> Geminiへ1回

はかなり合理的。API呼び出し回数もトークンも減る。

### 一方、Gemini案でワイがそのまま採用しない部分

**GitHubをSingle Source of Truthにするところ。**

ここはやっぱりワイは反対寄り。

Julesだけなら非常に綺麗。でもAIBOには、

**ローカルPC、OBS、VOICEVOX、マイク、Vision、Windows環境、実機テスト**

がある。

Claude/Codexがローカルで「まだcommitしてない状態」を調査している最中に、GitHubだけを真実とすると情報が欠ける。

だから今のところワイは、
```
司令塔DB
├ Task状態
├ Agent状態
├ Approval状態
├ 監査結果
└ 作業履歴

GitHub
├ Commit
├ Branch
├ PR
└ 共有成果物

PC
├ 実機環境
├ 未commit作業
├ OBS/VOICEVOX
└ ローカルテスト
```

の**三層案を維持**する。

GitHubは「コード成果物のSSOT」なら賛成。\
**システム全体のSSOTにはしない。**

あとGeminiの「Free APIは私有コードには重大な漏洩リスクだからPaid必須」は、安全側にかなり強く振った表現だと思う。Free Tierのデータ利用条件には確かに注意すべきだけど、だからといって監視システム全部を即Paidにする必要まではない。

たとえばFree Geminiには、
```
ファイル名
変更行数
テスト結果
静的解析結果
秘密情報を除去したdiff要約
タスク仕様
```

だけ渡す方法もある。

つまり**コードそのものを無料AIへ渡さない監査**も作れる。

ここはコスト削減上かなり重要。

そしてGemini案でちょっと笑ったのが、

> ローカル密着実装 → Claude Code\
> 非同期バックグラウンド → Jules

って**Google本人がClaude Codeをローカル担当に推薦してる**ところ。

そこは忖度薄くて好印象やｗ

---

現時点での採点をすると、

**ちゃぴお × Geminiで一致した部分はかなり信頼度上がった。**

特に、

**「一人が開発 → deterministic guard → 安いAI監査 → 必要なら強AI → 最後だけ相棒」**

これはもう本命候補にしていいと思う。

ただし**まだ設計決定しない。**

Claude案が来たら面白いところは、

**Claude自身がHooksを中心に据えるのか、外部司令塔を中心に据えるのか**。

もしClaudeまで「外部の軽量オーケストレータを中心にして、自分はworkerの一人にしろ」と言ってきたら、三者一致になる。

逆に「Claude Code Hooks/Agent SDKを司令塔の中核にしろ」って言ってきたら、そこで初めてちゃんと比較する価値が出る。

なので現在、

**ちゃぴお案：封印済み ✅**\
**Gemini案：受領・封印 ✅**\
**Claude案：待ち ⏳**

や。

Claude来たら**三者会議開廷**やな。相棒は貼るだけでええ。

# AI開発司令塔:Claude Code視点からの調査結果

先に結論だけ言うと、**「Claude Code を外部プログラムから完全リモコン操作する」こと自体は公式にほぼ全項目カバーされています**。一方で「Claude.aiの無料/定額チャット枠を自動化システムに正式流用する」道は現時点(2026年9月)で存在しません。以下、順に。

---

## 1〜2. Claude Codeを司令塔から操作する公式手段/役割分担

| レイヤー役割ステータス                                         |                                                                                                                                                         |                                                                                         |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| **Claude Agent SDK**(Python/TS, `claude-agent-sdk`) | プログラムから`query()`を呼び、ツール・エージェントループ・フック・サブエージェント・MCPを完全制御。Claude Code CLIバイナリを内部で保持。                                                                      | **公式に現在可能**(司令塔実装の第一候補)                                                                 |
| **Claude Code CLI ヘッドレスモード**(`claude -p`)           | シェルから1コマンドで実行。`--output-format json/stream-json`、`--resume`/`--continue`でセッション継続、`--json-schema`で構造化出力、`--allowedTools`/`--permission-mode`で権限制御。       | **公式に現在可能**(SDKの薄いラッパー、CI/CDに最適)                                                        |
| **Hooks**                                           | ツール呼び出し前後・停止時・サブエージェント完了時などに任意の外部スクリプトを実行し、許可/拒否/ブロックを返す仕組み。                                                                                            | **公式に現在可能**                                                                             |
| **MCP**                                             | Claude Codeは**MCPクライアント**(外部ツールを取り込む)にも**MCPサーバー**(`claude mcp serve`で自分のBash/Read/Editツールを他のMCPクライアントに公開)にもなれる。ただし自分が使っているMCPサーバーの中身はプロキシしない(パススルー不可)。 | **公式に現在可能**(用途は「Claude Codeを他エージェントのツールとして埋め込む」側に近く、司令塔のメイン制御経路としてはAgent SDK/`-p`の方が素直) |
| **GitHub Actions / GitLab CI/CD**                   | `anthropics/claude-code-action`はAgent SDKの上に構築された公式アクション。`@claude`メンション起動、または全PR自動レビュー(GitHub Code Review機能)。                                           | **公式に現在可能**                                                                             |

**推奨構成**: 司令塔の中核はAgent SDK(Python)で実装し、CIやシェル起点の呼び出しは`claude -p`を使う、というのが公式ドキュメントが示す標準的な役割分担です。

---

## 3. 自動化カバレッジ表

| 項目Claude CodeCodex CLIJules |                                                |                                         |                                               |
| --------------------------- | ---------------------------------------------- | --------------------------------------- | --------------------------------------------- |
| task開始                      | ✅ `query()`/`claude -p`                        | ✅ `codex exec`                          | ✅ REST `sessions.create`                      |
| prompt送信                    | ✅ 引数/stdin                                     | ✅ 引数                                    | ✅ APIボディ                                      |
| repo読取                      | ✅ Read/Grep/Glob(ローカルclone)                    | ✅ ローカル/クラウドsandbox                      | ✅ GitHub連携(専用VM)                              |
| file edit                   | ✅ Edit/Write                                   | ✅ apply\_patch                          | ✅(PRとして出力)                                    |
| bash/test実行                 | ✅ Bashツール(サンドボックス可)                            | ✅ OSネイティブsandbox                        | ✅ 設定次第でテスト実行                                  |
| progress取得                  | ✅ `stream-json`イベント(トークン単位)                    | ✅ `--json`ストリーム                         | ✅ Activities ポーリング/webhook                    |
| result取得                    | ✅ JSON result・session\_id・**cost**まで           | ✅ 最終応答/exit code                        | ✅ PR URL・成果物                                  |
| session継続                   | ✅ `--resume`/`--continue`(セッションID、別ディレクトリからも可) | ▲ `codex exec resume`(バージョン依存、要確認)      | ✅ 同一sessionへのmessage送信で自然に継続                  |
| stop/block                  | ✅ SIGINT/`interrupt()`、Hookでの即時Deny/Block      | ✅ 承認ゲートでのdeny                           | ▲ 初回プラン承認は可、実行途中の細粒度ブロックは限定的(要検証)             |
| human approval              | ✅ `PermissionRequest`フック+`canUseTool`コールバック    | ✅ 承認モード(`workspace-write`/`untrusted`等) | ✅ `requirePlanApproval`+`approve-plan`エンドポイント |

3社とも\*\*「プランの承認」までは公式APIで止められる**が、Claude Codeだけが**ツール呼び出し1回1回\*\*という最も細かい粒度でリアルタイム介入できる点が構造的に異なります(Hooksの仕組みがあるため)。

---

## 4. PreToolUse/PostToolUse/Stop/Subagentでの監視構成

Claude Codeのフックイベントは30近くありますが、司令塔設計で使うのは主にこの5つです。

- **PreToolUse**: ツール実行**前**。`exit code 2`または`{"decision":"deny"}`でブロックし、理由をClaudeにフィードバック。`.env`読取禁止のような**決定的ルール**をここに置く。
- **PostToolUse**: ツール実行**後**。差分をログ・監視AIに送るポイント。
- **PermissionRequest**: 権限ダイアログが出た瞬間をフック。ここで人間承認キューへの転送や監視AIによる自動判断を挟める。
- **Stop**: エージェントが「終わった」と言った瞬間。`StopHookActive`フラグで無限ループを防ぎつつ、「未解決issueがあるなら`Block`で理由を返し継続させる」という公式パターンがある。
- **SubagentStop**: サブエージェント(`.claude/agents`で定義するレビュー役など)が完了した時点。

フックは**標準入出力でJSONをやり取りする外部プロセス**にすぎないので、言語もロジックも自由です。ここが次の質問の答えに直結します。

---

## 5. Claude Code の Hook から Gemini / ChatGPT を監視役として呼べるか

**公式に現在可能**です。Hookは「任意のコマンドを実行し、JSON(またはexit code)を返す」だけの仕組みなので、そのスクリプトの中でGemini APIやOpenAI APIを叩いて判定結果を`allow`/`deny`/`block`にマッピングするのは、Anthropicのドキュメントが想定している正規の使い方の範囲内です。ただしAnthropicが「Geminiと連携するフック」を提供しているわけではなく、**中身は自作**になります。
```
PreToolUse (Bashコマンド実行前)
  → deterministic rule engine (regex/許可リスト) でまず判定
  → 判定不能な場合のみ Gemini Flash / GPT-5-mini などを呼んで WARN/BLOCK/PASS
  → JSON {"decision":"deny","reason":"..."} をstdoutに返す
```

---

## 6. Claude を Jules / Codex の監視役に使う方法

Claude Codeのようなツール単位フックはJules/Codex側に**公式には存在しない**ため、Claude側から見て取れる介入ポイントは基本的に**PR/diff単位**になります。

- **工夫すれば可能**: JulesのActivities webhook / Codexの`--json`出力を司令塔が受け取り、生成された差分(diff)をClaude API(Sonnet/Opus)に渡してレビューさせ、結果をJulesの`requirePlanApproval`承認APIやGitHubのレビューAPIに橋渡しする。
- **不可能/未提供**: JulesやCodexの内部ツール呼び出し1回1回に対して、Claudeがリアルタイムに介入すること(そもそもそのためのフックがJules/Codex側に公開されていない)。

つまり「Claude Codeは自分自身のツール実行をリアルタイムに他AIへ晒せるが、他社エージェントは同じ粒度の穴を空けていない」という非対称性があります。設計上、**PR境界での事後レビュー**に統一するのが現実的です。

---

## 7〜10. Claude.aiチャット枠 vs API vs Claude Code定額枠(最重要・時事的な話)

ここは公式サポートページ(2026年9月時点の最新表示)で確認しました。

> **2026年6月15日に「Agent SDK /&#x20;****`claude -p`****利用分を、対話利用(Claude.ai・Claude Code対話・Cowork)の使用上限から切り離し、別枠の月次クレジットにする」変更が予告されましたが、同日6月15日に一時停止(pause)されています。現時点(本稿執筆時)では変更前の状態のまま**で、Agent SDK・`claude -p`・サードパーティ製アプリの利用は、依然として**Claude Pro/Max/Team/Enterprise契約の対話利用上限と同じプールを消費**します。この状態がいつまで続くかはAnthropicの発表待ちです。

これを踏まえて:

| 質問回答                                       |                                                                                                                                                                                                                           |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Q7** Claude.aiチャット枠とAPI枠の関係              | 別会計。Claude.ai(Pro/Max/Team/Enterprise)は**サブスクの共有利用上限**、Claude Platform APIは**従量課金**。両者は独立していて、サブスク上限を使い切ってもAPI課金には影響しない(逆も然り)。                                                                                            |
| **Q8** Claude.aiの未使用無料枠を自作監視システムから正式利用する方法 | **不可能/未提供**。Claude.ai Web UI自体には公開APIがなく(あなたも除外方針と明記の通りWeb UI自動操作は規約違反)、「チャットの余り枠」をプログラムに回す公式手段は存在しません。唯一の公式な自動化経路は**同じサブスクの中でClaude Code/Agent SDKをヘッドレス実行すること**ですが、これは「余り枠の流用」ではなく「同じ共有プールを対話利用と食い合う」形になります。            |
| **Q9** 監視AIとしてのAPI最小コスト構成                  | **決定的ルールで大半を弾き、LLM判定は最小限**(後述13章)。呼ぶ場合はClaude Haiku 4.5のような軽量モデルをWARN/BLOCKの一次判定に、Sonnet/Opusは複雑な判断が要る差分だけに限定するのが最小コスト構成。                                                                                                 |
| **Q10** Claude Code定額枠を司令塔経由の自動処理に使える範囲    | 現状(前述の通り一時停止中)、`claude -p`/Agent SDK経由の自動処理は**サブスクの対話利用上限をそのまま消費**します。個人の実験・軽い自動化には使えますが、Anthropic自身が「**チームでの本番規模の自動化にはAPIキーによるClaude Platform(従量課金)を使うこと**」を推奨しています。定額枠を大量の自動処理に回すと対話利用(人間がターミナルでClaude Codeを使う分)を圧迫します。 |

---

## 11. GitHub中継でCodex・Claude Code・Julesを協調させる方法

**公式に現在可能**、かつ実際にこれが最も現実的な統合ハブです。

- Claude Code: `anthropics/claude-code-action`(公式GitHub Action)。`@claude`メンション or 全PR自動レビュー。
- Codex: `codex exec --full-auto`をGitHub Actionsのステップとして実行(公式CLI、OpenAI提供)。
- Jules: `automationMode: AUTO_CREATE_PR`でセッション開始時に自動PR作成を指定可能(公式REST API)。

3者とも\*\*「ブランチを切って作業しPRを開く」という共通言語\*\*で動けるため、GitHubを同期レイヤーにするのが最も摩擦の少ない設計です。各エージェントが直接会話する必要はなく、司令塔がGitHub上のPR状態(open/checks/reviews)をポーリングまたはwebhookで監視し、次のエージェントを起動するだけで済みます。

---

## 12. 他AIが作ったdiffをClaudeが自動レビューする方法

**公式に現在可能**です。パターンは2つ:

1. **GitHub Actions経由**: Codex/JulesがPRを開く → `claude-code-action`が`pull_request`イベントで起動し、diffを読んでレビューコメントを投稿(Anthropicの公式「GitHub Code Review」機能)。
2. **API直接呼び出し**: 司令塔が`git diff`をClaude API(またはヘッドレス`claude -p`)にパイプし、構造化出力(`--json-schema`)でPASS/WARN/BLOCKを取得。

---

## 13〜14. Deterministic-first設計とWARN/BLOCK時のフロー制御

これは技術的には「Hookの中身の設計思想」の話で、Anthropic製品というより**あなたが実装する部分**ですが、公式に用意されているプリミティブで十分組めます。
```
PreToolUse Hook
 ├─ ①決定的ルール(allowlist/denylist, パスパターン, 危険コマンド正規表現)
 │    → 該当すれば即 deny/allow、LLMは呼ばない(コスト・レイテンシ最小)
 └─ ②判断が必要なケースのみ
      → 軽量LLM(Haiku等)で一次判定 → 疑わしい場合のみ上位モデルへエスカレーション
```

WARN/BLOCK時の分岐(Stop / PermissionRequest フックで実装):

- **BLOCK** → `{"decision":"block","reason":...}`でエージェントに差し戻し、Claudeに自己修正させる(Stopフックの「未解決なら継続させる」公式パターン)
- **WARN(N回リトライしても解消しない)** → PermissionRequestフックで人間承認キュー(Slack/GitHub Issue等)へ転送し、`--permission-prompts none`環境では自動denyしつつ理由をログに残す
- **PASS** → CIテストへ

---

## 15. 見落としがちなAnthropic側の機能

- **GitHub Code Review**(自動全PRレビュー、`@claude`メンション不要の常時レビュー機能)— 監視AI層をAnthropicが半分肩代わりしてくれる。
- **GitLab CI/CD向けの公式インテグレーション**もGitHub Actionsと同格で存在。
- **Bedrock/Vertex AI/Microsoft Foundry経由のプロバイダルーティング**— Agent SDKはAPIキー以外にこれらも公式サポートしており、コスト配分・リージョン要件がある場合に選択肢になる。
- **Claude Code Plugins/マーケットプレイス**— Hook+Subagentのセットを配布可能な形にパッケージ化でき、司令塔ロジックを社内で使い回しやすい。
- `claude mcp serve`でClaude Code自体をMCPツールとして他クライアントに公開できる(ただし前述の通りパススルー制限あり)。

---

## 開発AIと監視AIを同一ベンダーにする場合 vs 別ベンダーにする場合

| 同一ベンダー(例: Claude Code開発+Claude監視)別ベンダー(例: Claude Code開発+Gemini/GPT監視) |                                                                                                          |                                                                                                                               |
| --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 長所                                                                    | 認証・課金・レート制限が一元化。フォーマット/セッション仕様の親和性が高くエラーハンドリングが単純。同一モデルファミリ内での文脈共有(自己レビューの一貫性)がしやすい。                     | **モデルバイアスの相互排除**が効く(自分が書いたコードを自分だけで採点しない)。単一ベンダー障害・規約変更(今回のAgent SDK課金一時停止のような)への耐性。異なる訓練データ/価値観による多角的レビューでハルシネーション見落としを減らせる。 |
| 短所                                                                    | 「同じクセ」を持つモデルが開発・監視双方を担うため、共通の盲点(同じ種類のミス)を見逃しやすい。単一ベンダーのAPI障害・仕様変更・利用規約変更(まさに現在進行形のAgent SDK課金の件)が全系統に波及。 | API/認証/フォーマットがバラバラで統合コストが増える。レイテンシとコストが増加(呼び出し先が増える)。ベンダー間のツール呼び出し粒度の非対称性(6章で述べた通りJules/Codexにはリアルタイムフックがない)を吸収する設計コストがかかる。   |
| 推奨                                                                    | コスト・速度重視の一次ゲート(決定的ルール+軽量LLM)には同一/低コストモデルでよい。                                                             | **最終ゲート(人間承認直前の最後の砦)は異ベンダーにする**のが堅牢性の観点で妥当。単一ベンダー依存の集中リスクを避けられる。                                                              |

---

## Anthropicの立場から見た、私ならこう設計する構成

ベンダーニュートラルに評価した上での提案です。
```
人間
 │
 ▼
司令塔(Python, Claude Agent SDK + 各社REST/CLIラッパー)
 ├─ Codex   → `codex exec --json --full-auto`(subprocess管理、session id保持)
 ├─ Claude Code → Agent SDK `query()` (hooks/subagent/MCP内蔵、最も細粒度の制御)
 └─ Jules   → REST API (sessions/activities, requirePlanApproval=true)
       │  (各エージェントは専用featureブランチで作業しPRを開く/更新)
       ▼
GitHub(唯一の共通言語・状態の単一情報源)
       ▼
監視/レビュー層(2段階)
  Stage 1: 決定的ルールエンジン(自作、社内ポリシーaslintルール)
           → 危険操作(rm -rf, secrets読み書き, 本番デプロイコマンド等)は問答無用でblock
  Stage 2: LLMレビュー(Stage1を通過したものだけ)
           → Claude(自己/他者コード双方をレビュー、Sonnet基準・複雑なものはOpus)
           → 高リスクPRのみ追加でGemini/GPTにセカンドオピニオンを取り、
             食い違ったら自動的に人間承認へ(コンセンサス方式、常時は呼ばない=コスト最小)
       ▼
自動テスト(CI、GitHub Actions上、Claude Code Actionと同一ワークフロー内で実行)
       ▼
分岐:
  全PASS                    → 自動マージ候補としてマーク(最終承認は人間 or 低リスク基準ならAuto)
  BLOCK/テスト失敗           → 該当エージェントに差し戻し(Stop hookのBlockパターンで自己修正、上限N回)
  N回失敗 or 高リスクパス該当 → 人間承認キュー(Slack/GitHub Reviewへ集約)
```

**設計思想の要点**:

1. Claude Code自身の細粒度Hook機構は司令塔の「神経系」として使い、Codex/Julesとの協調は**GitHubのPR境界**という粗い粒度に統一する(非対称性を無理に埋めようとしない)。
2. LLM呼び出しは**最後の手段**にする。決定的ルールで9割弾ければ、監視AIのコストもレイテンシも劇的に下がる。
3. 監視AIは**開発と同一ベンダーだけに依存させない**。特に人間承認直前のゲートは異ベンダーの意見を混ぜることで、単一モデルの盲点・単一ベンダーの規約変更リスク(まさに今回のAgent SDK課金一時停止のような出来事)から系全体を守る。
4. 本番規模の自動化は**Claude Platform APIキー**(従量課金)を軸にし、Pro/Max/Teamの定額枠は個人の実験・軽量タスクに留める—これはAnthropic自身が公式に推奨している運用方針です。

展開

よし、**三者出揃った。ここからは後出しでワイ案を書き換えず比較できる。**

結論から言うと、かなり強い結果になった。

**ちゃぴお・Gemini・Claudeが、中心思想ではほぼ同じ場所に着地してる。**\
ただしClaude案から、実装順を変えたくなるレベルの新情報が出た。

## 三者会議の結果

| 論点ちゃぴおGeminiClaude判定 |      |       |       |                |
| -------------------- | ---- | ----- | ----- | -------------- |
| 人間コピペを廃止             | ◎    | ◎     | ◎     | **全会一致**       |
| 外部オーケストレータ           | ◎    | ◎     | ◎     | **全会一致**       |
| 3AI常時監視              | ×    | ×     | ×     | **やらない**       |
| deterministicを最初に通す  | ◎    | ◎     | ◎     | **確定級**        |
| 軽量AI→強AI→人間          | ◎    | ◎     | ◎     | **確定級**        |
| Jules REST API       | ◎    | ◎     | ◎     | **採用候補**       |
| Claude Code外部制御      | ○    | ○     | **◎** | **想像以上に強い**    |
| GitHubを共通境界に         | ○    | **◎** | **◎** | **採用。ただし役割限定** |
| 異ベンダー監査              | ◎    | ○     | ◎     | **高リスク時採用**    |
| Gemini API Free監査    | ◎    | ◎     | ○     | **低機密用途なら有力**  |
| Chat無料枠の流用           | ×    | ×     | ×     | **捨てる**        |
| 人間承認                 | 最後だけ | 最後だけ  | 最後だけ  | **全会一致**       |

この時点で、

> **開発AIを別AIに監視させること自体は可能なの？**

への答えはもう、

# **可能。しかもかなり公式機能だけで組める。**

でいい。

---

## Claudeが持ってきた最大の新情報

これ。

> **Claude Agent SDK /&#x20;****`claude -p`****を外部司令塔から操作できる**

さらに、

> **PreToolUseで「実行する前」に外部監視AIへ問い合わせてBLOCKできる。**

これがデカい。

Julesの場合、
```
Jules
↓
作業
↓
Activity/diffを見る
↓
監査
↓
ヤバければ成果を捨てる
```

が基本。

Claude Codeなら、
```
Claude
↓
「このコマンド実行するで」
↓
Hook
↓
0円ルール判定
↓
必要なら別AI
↓
PASS ─→ 実行
BLOCK → 実行させない
```

ができる。

つまり、

**Jules = 隔離して作業させ、成果物を検査する**

**Claude Code = 作業中の手そのものを掴んで止められる**

という違いがある。

これ、監視方式を一種類に統一しようとしない方がいい。

---

# ワイ案から一個、順位を変更する
