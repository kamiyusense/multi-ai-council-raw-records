### GEMINI_R5-A_RAW.md
Multi-AI Council / Round 5-A
VENDOR: Google Gemini
MODEL: Gemini 3.1 Pro (User UI observed confirmation assumed)
ROUND5-A_BASE_COMMIT: 0a492405dafa14590232bc1dc297b43291d41d4b

### ============================================================
【設問1：考えの変遷・3社案の取捨・UNKNOWN】

**A. 維持した考え**
*   **Phase 0 の完全Read-only化と隔離ブランチの利用:** 危険操作を排除し、Jules等で検証する際は結果を破棄する隔離ブランチを用いる安全策。
*   **日本語UIによる1つの入力口:** ユーザーの操作負担を最小化するため、入力と結果表示は1つのローカルUI（日本語）に集約する方針。
*   **エラー駆動の停止（状態機械の受容）:** 利用枠の正確な事前予測が不可能である確定事実に従い、QUOTA等のエラーを受け取ってから一時停止（BUSY→ERROR）するフェイルセーフな設計。

**B. 修正・縮小した考え**
*   **Gemini自身の完全自動回収の前提:** Notebook（Google Drive）経由での参照は確認済だが、実行役（司令塔）からGeminiチャットへの完全な自動往復経路は実証されていない（UNKNOWN）ため、「Gemini分だけは半自動（貼り付け・回収補助）を許容する（L1縮退）」形へ修正。

**C. 撤回した考え**
*   **AIによる合議（裁定）と動的重み付け:** AIが他者の意見を要約・裁定することで生じる「伝言ゲーム」と「審査員の審査不能問題」を防ぐため、照合・差分抽出層（層4）からLLMを完全に排除し、決定論的コードのみに任せる形へ撤回。

**D. 他社から採用した考え**
*   **ChatGPT案から:**
    *   【採用】**Providerを状態機械（AVAILABLE / BUSY / QUOTA_OR_LIMIT_ERROR / DISABLED_BY_USER 等）で扱う設計。** ユーザーの温存判断（DISABLED）を正常系として扱う点が実用的であるため。
*   **Claude案から:**
    *   【採用】**L2（自動）→L1（半自動）→L0（手動）という明確な縮退構造。** 枠切れやAPI障害時でもユーザーが作業を止めずに済むため。
    *   【採用】**指紋（SHA-256 / Commit ID）の照合。** キャッシュ等の問題で古い内容を読み込む事故を防ぎ、正本同一性を機械的に担保するため。
    *   【採用】**実測と撤回の条件付け。** 実際に操作回数・時間が減らなければ撤回するという厳密な目標管理。

**E. 現在も一致していない考え**
*   【対立点】「CodexなどOpenAI側を、月額サブスクではなく従量課金API（API Key）を主経路として想定するかどうか」
    *   **理由:** 私は初期段階では月額枠（ゼロ円）を優先すべきだと考えていたが、ChatGPTのRound 4-B回答（WorkとCodexが枠を共有する自社不利の告白）や、自動化の確実性を考慮すると、OpenAI側については「月額枠の枯渇を避けるため、少額のAPI課金を許容する」方が、実運用上は安定する可能性がある。しかし、ユーザー要件（追加費用をかけたくない）と衝突するため、完全な合意には至っていない。本案では「明示的なフォールバック」として残す。

**F. UNKNOWNのまま残ったもの**
*   **月額枠のプログラムからの正確な残量取得**
    *   **なぜ:** 各社とも公式な取得手段（API）が存在しないため。
    *   **何があれば:** 各ベンダーから正式な使用量取得エンドポイントが提供されれば。
    *   **今回の扱い:** 【暫定運用（安全側に倒す）】残量予測は行わず、Quotaエラーを受信した時点で当該AIを一時停止状態（QUOTA_OR_LIMIT_ERROR）にするエラー駆動で運用。
*   **Geminiの完全自動回収（L2）の成否**
    *   **今回の扱い:** 【保留（L1へ縮退）】安全と確実性を優先し、Geminiの処理は手動コピペ（L1）を基本とする。

### ============================================================
【設問2：正式運用候補 ― Day 1】

**■ 構成要素（Day 1）**
ユーザーのWindows PCローカルで動作する、Pythonスクリプトベースの**「状態機械型・指紋照合付き Orchestrator（L1/L2混在型）」**。

**■ なぜこの形がDay 1にふさわしいか**
追加費用ゼロ（月額枠の流用）を前提としつつ、過去の事故原因であった「配布漏れ」「同期ズレ（古いキャッシュ）」を指紋（SHA-256）で機械的に弾けるため。また、Geminiの自動化がUNKNOWNでも、L1（半自動）として全体のシステムを止めずにすぐ運用を開始できるため。

**#### 2-A. 最初の一回だけ必要な準備**
1.  Python環境のインストール（Windows上）。
2.  Orchestratorスクリプト（`council.py`）のダウンロード。
3.  Claude / Codex 用のローカル認証設定（`claude -p` のログイン認証、OpenAIのログイン等。※APIキーではなく月額用の認証）。
4.  JulesのSettingsからAPIキーを発行し、ローカル環境変数へ登録。

**#### 2-B. ユーザーがやること（1案件の流れ）**
1.  コマンドプロンプト等から `python council.py` を起動し、日本語で質問・指示を入力。
2.  スクリプトが生成した「Gemini用のプロンプト（コードブロック）」をコピーし、ブラウザのGeminiチャットへ手動で貼り付け。
3.  Geminiの回答をコピーし、スクリプトへ貼り付けて戻す（Gemini分のL1操作）。
4.  （Claude, Codex/Julesの回答は裏でスクリプトが自動回収し、指紋照合・差分抽出を行う）
5.  生成された「Markdownレポート1枚（一致点・相違点・UNKNOWN）」を読む。
6.  最終的な判断（次へ進む、または危険操作のMerge承認）を入力する。

**#### 2-C. 機械がやること**
1.  入力とプロンプトの各AIへの配布（Gemini以外は自動送信）。
2.  取得した正本のコミットIDとSHA-256の照合（不一致なら警告）。
3.  URLと主張の抽出、および「結論一致度」と「根拠URLの一致度」の4象限分類。
4.  Providerの状態管理（AVAILABLE、BUSY、QUOTA_OR_LIMIT_ERRORなど）。
5.  各社RAWのローカル保存（`.md`）およびログ追記（`.jsonl`）。
6.  レポートのMarkdown生成。
※ AIによる推論（LLMの呼び出し）はここには一切含まれない。

### ============================================================
【設問3：Day 2以降（Phase 0完了後）の発展】

Day 1の実測を経て、ユーザーの操作時間削減が証明された後、**「Phase 1：安全な書き込みの実現」**へ進みます。
*   **追加・拡張する部分:** GitHub APIを利用した、Orchestratorからの「隔離ブランチ（`council-review` 等）への自動Pull Request作成機能」。
*   **ユーザー作業の何が減るか:** AIが提案した修正コードを、ユーザーが手動でコピペして手元で修正・コミットする手間。ユーザーの作業は「Orchestratorが作ったPRをブラウザで見て、Mergeボタンを押すだけ」に変わります。

### ============================================================
【設問4：お金の扱い】

*   **基本の課金状態:** 追加のAPI費用は**「0円」**。
*   **使う枠:** Claude Agent SDK（Claude Pro枠）、Codex CLI（ChatGPT Plus枠）、Jules（Google AI Pro枠）。
*   **課金事故を防ぐ仕組み:** 実行前にローカルの環境変数をスキャンし、`ANTHROPIC_API_KEY` 等の従量課金用キーが設定されていれば、スクリプトが起動をハードブロック（エラー停止）する仕組みを入れます。

### ============================================================
【設問5：設計目標と仮の閾値】

*   **設計目標（操作回数）:** 1案件あたり **4〜6回**。
    （内訳：開始指示1 + Gemini貼付/回収2 + レポート確認1 + 承認・その他1〜2）
*   **仮の閾値（定足数/Quorum）:**
    「**2社以上の有効な回答**」が得られ、かつ「**安全性の警告が0件**」であることをCouncilの合意成立の閾値とします。
    （1社が枠切れで欠席しても、残り2社で一致すれば「合意」としてユーザーへ返す。ただし、誰かが危険と判定した場合は定足数に関わらず人間へエスカレーションする）。

### ============================================================
【設問6：自分の案を壊す・撤回条件】

**■ 最も破綻しやすい弱点3つ**
1.  **弱点:** 「GeminiのL1手動コピペがユーザーの負担になりすぎる」
    *   **なぜ危険か:** 「作業を減らす」という本来の目的が達成されないため。
    *   **どの条件で問題か:** 1案件の中で細かいやり取り（Round）が複数回発生した場合。
    *   **Phase 0での検証:** 1ラウンド回した際の操作回数と体感時間を実測する。
    *   **修正/撤回条件:** 実測の結果、現行の手動コピペ運用より操作回数や時間が減っていなければ、この司令塔案の「配布・収集の半自動化部分」を撤回し、L0（完全手動＋照合スクリプトのみ）へ後退します。

2.  **弱点:** 「CLIツール（Agent SDK / Codex）が月額枠を急激に食いつぶす」
    *   **なぜ危険か:** ユーザーが日常のAIとの会話（アイデア出し等）に使うための枠が枯渇し、本末転倒になるため。
    *   **どの条件で問題か:** Claude Code等がバックグラウンドで大量のツールコールをループさせた場合。
    *   **Phase 0での検証:** レポート生成のたびに、各プロバイダのダッシュボードで消費%（または回数）を人間が目視確認して記録する。
    *   **修正/撤回条件:** 1案件あたりの消費が枠の10%を超えるようなら、該当ツールの使用を「人間が許可した場合のみ」に修正（条件付き自動化）します。

3.  **弱点:** 「指紋（SHA-256）照合による偽陽性ブロックが多発する」
    *   **なぜ危険か:** AIが改行コードの差異（CRLFとLF）やMarkdownの微細なパース違いを起こすだけで、「正本と違う」と判定されシステムが頻繁に止まるため。
    *   **どの条件で問題か:** AIの出力フォーマッタが不安定な場合。
    *   **Phase 0での検証:** 返ってきたハッシュ値と実際の正本ハッシュの不一致率を測定する。
    *   **修正/撤回条件:** 不一致エラーが3割を超える場合、完全一致ではなく「コミットIDのみの照合」へ緩めるか、照合機構を撤去します。

### ============================================================
【設問7：実装開始判定】

**判定:** **B. 条件付きで進める。**
**理由:** 設計としては安全かつ現実的ですが、Geminiへの手動コピペ（L1）が発生することと、それによって「本当に時間が減るか」がまだ未知数（目標値でしかない）ため、1ラウンドだけ回して「実測」するという条件付きでの進行が妥当です。
**最初に実装する具体的な1ステップ:**
「3社の出力を受け取り、JSONスキーマを検証して、URLの一致/不一致を分類したMarkdownレポートを1枚生成する、**ローカルのPython関数（Comparator層）を1つだけ書く**こと」。

### ============================================================
【設問8：Round 5の収束に向けて】

① **他社から反論されても残すべき部分:** 「Orchestrator（司令塔）自身はAIを一切呼び出さず、決定論的コードで処理する（統合時のハルシネーション排除）」という原則。
② **他社案が優れていれば変更する部分:** Geminiの自動回収経路（L2）。他社またはユーザーが安全かつ無料枠・月額枠内で実現できる確実なスクリプト等を発見した場合は、手動（L1）を撤回して自動化（L2）へ変更します。
③ **他社2社も同じ結論なら共通案にする部分:** 「指紋照合による同一性担保」「エラー駆動の状態管理（AVAILABLE/QUOTA_ERROR等）」「Quorum（定足数2社）の導入」。
④ **最後まで一致しなかった場合、ユーザーへ返す論点:** 「OpenAI（Codex）を月額枠（CLI）で粘るか、安定性のために少額のAPI従量課金（1回数円）を許容するか」のコスト・安定性トレードオフの決断。
⑤ **統合案へ進むべきと判断する状態:** 3社すべてが「司令塔は決定論的（AI非依存）であるべき」「追加課金ゼロを基本とする」「安全操作（隔離ブランチ・人間承認）を必須とする」のコア3点で合意した状態になれば、細かいコードの実装仕様の差はあれど、統合案として提出可能です。
