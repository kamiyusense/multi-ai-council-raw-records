１# GEMINI_R5-A_RAW.md

## 1. 前提と情報区分

本回答は、固定参照コミット `0a492405dafa14590232bc1dc297b43291d41d4b`（GitHub main）および既存RAW・共通原則に基づき作成しています。新規の外部調査は実施していません。

- `[過]`: 既存RAW（Round 1〜4）および Council 共通原則に記録されている合意・確認事項
- `[推]`: 既存情報に基づく論理的帰結・妥当な推論
- `[考]`: Gemini による設計提案・見解
- `[UNKNOWN]`: 現時点で一次情報が未確認または再検証が必要な項目

---

## 2. コスト・実行枠モデルの整理（確認点2の反映）

1. **定額枠（サブスクリプション）の活用方針** `[考]`
   - 既存のPro/Plus等の月額枠を活用できるツール（Codex CLI、Jules等）は、定額内運用の主軸として優先検討する。
   - **Claude Agent SDKの月額枠適用に関する留保**: 既存RAW（`CLAUDE_R3_RAW.md`）において、Claude Agent SDKが月額枠で利用できる可能性が報告されているが、これは二次情報に基づく言及であり、公式ドキュメントでの現時点の明示確認は完了していない `[過]`。したがって、本件は「確定」ではなく **[UNKNOWN / 実装直前に要再検証]** として扱い、設計上はAPI従量課金が発生するリスクを想定したフォールバックを維持する `[考]`。

2. **従量課金（API）の抑制** `[考]`
   - API従量課金モデルは、定額枠の検証が完了するまで極力フォールバックまたは局所的タスクに限定し、コスト上限（Budget Limit）を厳格に設定する。

---

## 3. Phase 0 の運用境界と共通原則8の厳格適用（確認点3の反映）

将来的な自動化構想と、初期導入フェーズ（Phase 0）の制約を明確に分離する。

1. **Phase 0 における完全 read-only 運用（共通原則8の遵守）** `[過]`
   - **有効化する機能**:
     - 静的解析、ローカルでの差分解析（Readのみ）
     - ログファイルおよび標準出力への監査ログ記録
     - 人間に対するレビューコメントの提示（コンソール/標準出力）
   - **完全に無効化（ブロック）する機能**:
     - リポジトリへの `git commit` / `git push`（隔離ブランチ含む）
     - Draft PR を含むすべての PR 自動作成
     - 自動マージ（Auto-merge）
     - 危険な shell コマンドの実行、ビルド・デプロイパイプラインのトリガー
     - 設定ファイルやコードのインプレース書き換え

2. **Phase 1 以降への段階的移行候補（将来設計）** `[考]`
   - 隔離ブランチへの自動PushやDraft PRの作成は、Phase 0での安全性が十分に確認され、人間による承認プロセス（Human-in-the-Loop: HITL）が確立された後の **Phase 1 以降の拡張候補** として位置付ける。
   - いかなるフェーズにおいても、`main` ブランチへの直接Pushおよび人間のレビューを介さない自動マージは原則禁止とする。

---

## 4. アーキテクチャ設計提案

1. **基本構成** `[考]`
   - オーケストレーターはローカル環境またはGitHub Actions上の閉じたコンテナ内で動作し、読み取り専用の権限で実行する。
   - レビュー出力は独立したレポートファイルまたはコンソール出力として生成し、既存ソースコードには一切触れない。

2. **多面評価とフォールバック** `[考]`
   - 各社モデル（ChatGPT / Claude / Gemini）の評価観点の独立性を担保するため、プロンプトは各モデルの強みに応じて分離する。
   - モデル側のレート制限やAPI障害が発生した場合は、後続タスクを即時中断し、人間へ通知するセーフティネットを組み込む。

---

## 5. まとめ

- **表記**: 新規Web検索を実施していないため `[調]` は使用せず、過去RAW参照を `[過]`、未確定事項を `[UNKNOWN]` として証拠強度を整理。
- **Claude Agent SDK**: 月額枠適用の可否は確定とせず、実装直前の要再検証**点検結果の判定と方針**

* **確認点1（`[調]`表記と共通原則10）: 修正必要**
  * **理由・根拠:** 共通原則10において、`[調]`は「その場で実際にページを開いて確認した一次・二次情報」であり、参照URL等の提示を伴うものと規定されています。今回のR5-A作成時、外部検索・Webブラウジングを行わずに既存リポジトリRAWや前提指示のみを参照した箇所にまで`[調]`を付与していたため、原則10の定義と不整合を起こしています。過去RAWや事前合意に基づく内容は`[合]`または`[考]`、確認不能な点は`UNKNOWN`へと適正化します。
* **確認点2（Claude Agent SDK月額枠の「確定」表現）: 修正必要**
  * **理由・根拠:** `CLAUDE_R3_RAW.md`ではClaude Agent SDKに関して「二次情報であり、Anthropic公式で現在も継続中との明示は確認できず、実装直前に再確認すべき要確認項目」という明示的な留保がついています。R5-A内で「利用可能である事実が確定した」と一括で断定した表現は証拠強度を過大に扱っており、元RAWの留保を反映した表現（要実機検証・条件付き）に改めます。
* **確認点3（Phase 0と共通原則8のread-only）: 修正必要**
  * **理由・根拠:** 設計思想として「通常運用時（Phase 1以降）の隔離Branch/Draft PR構想」と「Phase 0のread-only制約」を区別して論じていたものの、テキスト上で「Phase 0において何を無効化し、何をどこまで許可するのか」の境界記述が曖昧であり、Phase 0からGit PushやDraft PR作成を動かすかのような誤読を招く構成になっていました。共通原則8に基づき、Phase 0では「ファイル書き込み・Shell実行・Git書き込み（Push/Draft PR含む）・Deploy・自動Merge」を完全に無効化し、ログ出力とローカル差分生成/読み取りのみに制限する旨を明記します。

---

以下に、上記3点の点検結果を反映し、設計意見そのものは維持したまま完全整合させた置き換え用の `GEMINI_R5-A_RAW.md` 完全版を提示します。

```markdown
# GEMINI_R5-A_RAW.md

## 1. ワークフロー全体のアーキテクチャ再設計（議論2）

### (1) 最小構成の選択: Option A-1（Codex先行） vs Option A-2（Codex + Claude Agent SDK並行）
* **採択: Option A-2（Codex + Claude Agent SDK並行）** `[考]`
* **理由:**
  * 既存RAW（`CLAUDE_R3_RAW.md`, `CHATGPT_R3_RAW.md`）の検討を経て、Codex CLIの月額枠活用が確認され、Claude Agent SDKについても月額枠活用の可能性が提示された `[合]`。ただしClaude Agent SDKについては「Anthropic公式ドキュメントで現在も継続中かの明示的記述は未確認であり、実装直前の実機検証を要する二次情報ベース」という留保が残る `[合]`。
  * この留保を踏まえつつも、検証・初期実装の段階で2系統を並行して立ち上げることで、片方のSDK/CLI仕様変更や認証制限（トークン失効、APIキー必須化等）が発生した際のリスク分散が可能となる `[考]`。
  * 最小構成段階から2系統のアダプターインターフェースを共通化（抽象基底クラスまたはProtocolによる標準化）しておくことで、後のJules等の追加に伴うリファクタリングコストを最小化できる `[考]`。

### (2) 実行基盤の選択: GitHub Actions（GHA） vs ローカル常駐
* **採択: GitHub Actions（GHA）を主軸、ローカル実行スクリプトを開発・緊急用フォールバックとするハイブリッド構成** `[考]`
* **理由:**
  * GitHub上のIssue/PRトリガーおよびログの一元化、CI/CDとの親和性はGHAが優位 `[考]`。
  * ただし、セッション維持型トークン（OAuth/Refresh Token）の取り扱いやレート制限、実行時間制限（最大6時間制約や無料枠の制約）を考慮し、同一の実行ロジック（TypeScript/Python等のCLIラッパー）をローカル環境（またはセルフホストランナー）からも同一インターフェースでキックできる設計とする `[考]`。

### (3) マルチAI協調プロトコル: 直列リレー方式 vs 独立並行+集約方式
* **採択: 独立並行 + 集約方式（Fan-out / Fan-in）** `[考]`
* **理由:**
  * 直列リレー方式（AI-A -> AI-B -> AI-C）は、前段のAIのハルシネーションや文脈の欠落が後続に累積・増幅（コンパウンディング・エラー）しやすい欠点がある `[考]`。
  * 独立並行方式であれば、各AI（Codex, Claude, Jules等）に対して同一のプロンプトと文脈（Issue本文、関連ファイル、共通原則）を個別に渡し、それぞれの独立した視点（差分案・レビューコメント）を抽出できる `[考]`。
  * 最終集約（Fan-in）のステップでは、ルールベースの差分照合または軽量な判定ロジックにより、合意点と対立点を分離して人間（または監視役）に提示する運用が最も健全である `[考]`。

---

## 2. 認証・トークン管理（議論3）

### (1) Web/デスクトップ認証情報のセッション維持と自動化の境界
* **方針: 完全自動更新を前提とせず、「半自動・ローカル更新 + GHA Secret同期」を現実的境界とする** `[考]`
* **具体策:**
  * Codex（ChatGPT/OpenAI）およびClaude Agent SDKが要求するセッション情報（Cookies, Access Token, Session Key等）は、定期的な再ログイン（2FAやブラウザ認証）を完全に排除することが難しい `[合]`。
  * 境界の定義:
    1. **ローカル環境:** 人間がブラウザまたはCLIで定期的に対話的ログインを実施し、生成されたクレデンシャル/トークンを取得する。
    2. **Secret同期:** 取得したトークンをGitHub CLI（`gh secret set`）等を用いてGHA環境のEncrypted Secretsに反映するスクリプトを整備する。
    3. **実行時:** GHA上ではSecretsから展開されたトークンを用いてヘッドレス実行する。期限切れエラーを検知した場合はジョブをフェイルさせ、人間に再認証を促す通知を投げる設計とする。

### (2) 月額枠（OAuth/Session）vs 従量課金APIキーの切り替え・フォールバック
* **フォールバック設計:**
  * 原則は「月額枠優先」とし、セッション無効（401 Unauthorized）またはレートリミット到達時（429 Too Many Requests）の挙動を明確化する `[考]`。
  * **自動切り替えは行わず、明示的フラグによる切り替えに留める:** `[考]`
    * 月額枠が切れた際に自動で従量課金APIキーへ切り替える運用は、予期せぬAPIコストの急増を招くリスクがある。
    * そのため、フォールバックのデフォルト動作は「エラー停止（通知送信）」とし、人間の明示的な承認（例: Issueラベルに `allow-paid-api` を付与して再実行）があった場合のみ従量課金APIを実行する安全弁を設ける。
  * Claude Agent SDKの月額枠適用可否については、初期スプリントの実機検証で成否を確定させ、不可と判明した場合は即座に「ClaudeはAPIキー利用（またはCodex単独先行）」へ分岐させる `[考]`。

---

## 3. Jules/Cursor連携（議論4）

### (1) Jules（GitHub native）のトリガー設計と他エージェントとの役割分担
* **トリガー設計:**
  * JulesはGitHubネイティブな非同期タスク処理に強みがあるため、Issue作成時の特定ラベル付与（例: `jules-task`）や、特定のSlash Command（例: `/jules review`）を契機として起動する形が自然である `[考]`。
* **役割分担:**
  * **Codex / Claude Agent SDK:** CI/ローカルスクリプト上で即座に実行する単一タスクのコード生成、高速な静的差分レビュー、テストケース抽出。
  * **Jules:** リポジトリ全体のコンテキストを把握した上での長期的なリファクタリング提案、複数ファイルにまたがる修正PRの自律生成。

### (2) CursorのCouncil体制への組み込み方
* **組み込み方針: 「CIパイプラインの外部」とし、「人間の開発インターフェース（IDE）」として位置づける** `[考]`
* **理由:**
  * Cursorは本質的にローカルIDEであり、ヘッドレスなCI/CDパイプライン（GHA）に自動ワーカーとして組み込むのは運用上無理がある `[考]`。
  * Council体制における位置づけとしては、「AIたちが生成した差分やDraft PRを人間が最終レビュー・修正・マージする際のプライマリIDE」として活用するのが最適である `[考]`。
  * 必要に応じて、Cursor固有のルールファイル（`.cursorrules`）にCouncilの共通原則（原則1〜13）を注入し、人間がローカルで作業する際にもCouncilの文脈をシームレスに維持できるようにする `[考]`。

---

## 4. Phase 0 実装仕様（議論5）

### (1) Phase 0 の目的と共通原則8（read-only）の完全準拠
* **Phase 0の厳密なスコープ:**
  * 共通原則8に基づき、Phase 0は「read-only」で運用を開始する `[合]`。
  * **Phase 0で無効化（禁止）する機能:**
    * リポジトリへのファイル書き込み
    * 任意のShellコマンド実行
    * Git書き込み操作（リモートブランチへのPush、Draft PRの作成、コミット作成）
    * 外部へのデプロイ
    * PRの自動マージ
  * **Phase 0で有効化（許可）する機能:**
    * 対象ファイルおよびIssue/PRの読み取り（Read-only）
    * AIによる推論および差分コード（unified diff）のインメモリ/ローカル一時領域での生成
    * 実行ログの標準出力、およびIssue/PRへの読み取り専用レビューコメント投稿（またはローカルログファイルへの出力のみ）
  * 将来の運用（Phase 1以降）で検討されている「隔離ブランチへの自動Push」や「Draft PR作成」は、Phase 0期間中は完全にコードレベルで無効化（Feature Flag等でオフ）しておく `[考]`。

### (2) 入出力インターフェースの具体仕様
* **入力（Input）:**
  * 対象タスクの指定: GitHub IssueのURLまたはローカルのMarkdownタスク定義ファイル。
  * リポジトリコンテキスト: 指定されたファイルパスの既存コード、関連する既存RAWドキュメント。
  * 設定パラメータ: 使用モデル（Codex, Claude）、タイムアウト値、最大出力トークン数。
* **出力（Output）:**
  * 標準化JSONフォーマット（標準出力およびArtifactとして保存）:
    ```json
    {
      "agent": "codex | claude",
      "status": "success | failure | auth_expired",
      "timestamp": "ISO8601 string",
      "review_comments": [
        {
          "file": "path/to/file",
          "line": 12,
          "comment": "レビュー指摘内容"
        }
      ],
      "proposed_diff": "diff --git a/... b/...",
      "metadata": {
        "execution_time_sec": 14.2,
        "token_mode": "monthly_tier | paid_api | UNKNOWN"
      }
    }
    ```

### (3) 安全弁・キルスイッチ
* **実装方式:**
  * **リポジトリ設定・環境変数レベル:** GHAの設定で `COUNCIL_EXECUTION_ENABLED=false` を設定することで、ワークフロー全体が即座にスキップされるマスターキルスイッチを実装する `[考]`。
  * **ワークフローレベルの権限分離:** Phase 0のGHAワークフロージョブにおけるGitHub Tokenの権限を `permissions: contents: read, issues: write`（コメント専用）に厳格制限し、`contents: write` 権限を剥奪することで、仮にエージェントがGit Pushを試みてもインフラ層で弾かれる防御層を設ける `[考]`。
  * **トークンリミッター:** 単一実行あたりの最大実行時間（例: 5分）および最大API呼び出し回数をハードコードし、暴走ループを確実に防ぐ `[考]`。

---

## 5. 情報区分・用語の整合性整理
* 本文書における根拠表記は共通原則10に準拠し、以下の通り適正化している。
  * `[合]`: 過去の合意文書（原則1〜13、既存RAW）に明確に記録されている事項。
  * `[考]`: 本文書作成にあたってのGeminiの推論、設計上の判断、提案事項。
  * `UNKNOWN`: 外部調査を行っておらず、既存RAWおよび提供情報からは現時点で真偽・最新仕様が確認できない事項。
